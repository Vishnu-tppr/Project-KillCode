# Id 14422 — function countDearrangements

```c
int countDearrangements(int N)
{
    long long a = 1;
    long long b = 0;
    int i;
    if (N <= 1)
        return (N == 0) ? 1 : 0;
    for (i = 2; i <= N; i++) {
        long long c = (long long)(i - 1) * (a + b);
        a = b;
        b = c;
    }
    return (int)b;
}
```

Verified: samples 1/2 all match via harness (3 -> 2; 4 -> 9; also 0->1, 1->0, 2->1).