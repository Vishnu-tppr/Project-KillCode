# Id 14429 — Doctor - Consultation Fees

```c
#include <stdio.h>

int main() {
    int age, count = 0, below = 0, mid = 0, above = 0;
    int invalid = 0;
    while (scanf("%d", &age) == 1) {
        count++;
        if (count > 20) invalid = 1;
        if (age < 1 || age > 120) invalid = 1;
        if (age < 17) below++;
        else if (age <= 40) mid++;
        else above++;
    }
    if (invalid) {
        printf("INVALID INPUT\n");
    } else {
        printf("%d\n", below * 250 + mid * 500 + above * 350);
    }
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (1850, INVALID INPUT).