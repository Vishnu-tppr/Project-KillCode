# Id 2621 — Series Team Score

```c
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main() {
    long long a[101], b[101];
    int na = 0, nb = 0;
    char line[512];
    fgets(line, sizeof line, stdin);
    char *t = strtok(line, " \n");
    while (t) { a[na++] = atoll(t); t = strtok(NULL, " \n"); }
    fgets(line, sizeof line, stdin);
    t = strtok(line, " \n");
    while (t) { b[nb++] = atoll(t); t = strtok(NULL, " \n"); }
    long long sa = 0, sb = 0;
    for (int i = 0; i < na; i++) {
        if (a[i] > b[i]) sa += 3;
        else if (a[i] < b[i]) sb += 3;
        else { sa++; sb++; }
    }
    printf("%lld %lld\n", sa, sb);
    return 0;
}
```

Verified: `3 5 1 / 3 2 0` → `7 1` (recorded sample output appends explanation text; program matches the stated output)