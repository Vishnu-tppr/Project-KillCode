# Id 7030 — Product or Sum of Three Integers

```c
#include <stdio.h>
int main(){
    long long X,Y,Z;
    scanf("%lld %lld %lld",&X,&Y,&Z);
    if(X<=Y&&Y<=Z) printf("%lld",X*Y*Z);
    else printf("%lld",X+Y+Z);
    return 0;
}
```

Verified: 7 8 3 → 18