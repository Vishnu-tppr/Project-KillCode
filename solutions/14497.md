# Id 14497 — Find Box Number - Counting Puzzle

```c
#include <stdio.h>

int main() {
    long long N;
    scanf("%lld", &N);
    int pat[8] = {1, 2, 3, 4, 5, 4, 3, 2};
    printf("%d\n", pat[(N - 1) % 8]);
    return 0;
}
```

Verified: samples 1/2 match via verify.py.