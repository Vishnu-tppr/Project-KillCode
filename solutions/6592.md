# Id 6592 — Distributed and Remaining Idlis

```c
#include <stdio.h>

int main() {
    int m, n;
    scanf("%d%d", &m, &n);
    printf("%d %d\n", m / n, m % n);
    return 0;
}
```

Verified: `10 2 → 5 0`, `15 7 → 2 1`
