# Id 12186 — function customCaesarCipher

```c
char *customCaesarCipher(int key, char *message) {
    if (key < 0) return "INVALID INPUT";
    int as = key % 26;
    int ds = key % 10;
    for (int i = 0; message[i] != '\0'; i++) {
        char c = message[i];
        if (c >= 'A' && c <= 'Z') {
            message[i] = (char)('A' + (c - 'A' + as) % 26);
        } else if (c >= 'a' && c <= 'z') {
            message[i] = (char)('a' + (c - 'a' + as) % 26);
        } else if (c >= '0' && c <= '9') {
            message[i] = (char)('0' + (c - '0' + ds) % 10);
        }
    }
    return message;
}
```

Verified: samples 1/2/3 all PASS via harness (Bmm jt xfmm. #56.0L Uxffut / Kssh fci... Qv. BCD / INVALID INPUT)