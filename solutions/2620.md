# Id 2620 — Message Encryption

```c
#include <stdio.h>
#include <string.h>
int main() {
    char m[201];
    int c;
    scanf("%s%d", m, &c);
    int len = (int)strlen(m);
    int rows = (len + c - 1) / c;
    char g[200][30];
    int k = 0;
    for (int r = 0; r < rows; r++) {
        if (r % 2 == 0)
            for (int col = 0; col < c; col++) g[r][col] = m[k++];
        else
            for (int col = c - 1; col >= 0; col--) g[r][col] = m[k++];
    }
    for (int col = 0; col < c; col++)
        for (int r = 0; r < rows; r++)
            putchar(g[r][col]);
    putchar('\n');
    return 0;
}
```

Verified: `midinadiazne / 3` → `madeinindiaz`; `loaesfbnaiordilertenrdhdw / 5` → `lionroaredandthebirdsflew` (recorded sample1 output appends explanation text; program matches the stated output)