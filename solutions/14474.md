# Id 14474 — Booking Ad - All Slots

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    int a[1000];
    for (int i = 0; i < N; i++) scanf("%d", &a[i]);
    int K;
    scanf("%d", &K);
    int ok = 1;
    for (int i = 0; i < N; i += 3) {
        if (a[i] != K && a[i + 1] != K && a[i + 2] != K) { ok = 0; break; }
    }
    printf("%d\n", ok);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (1, 0)