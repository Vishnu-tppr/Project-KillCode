# Id 6572 — Assignment Distribution

```c
#include <stdio.h>
int main() {
    long long x, y;
    scanf("%lld%lld", &x, &y);
    printf("%lld\n", x % y);
    return 0;
}
```

Verified: `159 10` → `9`; `346 5` → `1`; `340 20` → `0`