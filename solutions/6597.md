# Id 6597 — Certificates Remaining

```c
#include <stdio.h>

int main() {
    int m, y, w;
    scanf("%d%d%d", &m, &y, &w);
    printf("%d\n", y - (m + w));
    return 0;
}
```

Verified: `50 120 30 → 40` (recorded sample output additionally contains the statement's explanation lines, which the program is not required to print; the output value per the statement is `40`)
