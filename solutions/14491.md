# Id 14491 — Two Series - Common Term

```c
#include <stdio.h>

int main() {
    int n, p, q, r, s;
    scanf("%d", &n);
    scanf("%d", &p);
    scanf("%d", &q);
    scanf("%d", &r);
    scanf("%d", &s);
    int i, j;
    for (i = 0; i < n; i++) {
        long long a = q + (long long) i * p;
        for (j = 1; j < n; j++) {
            long long b = s + (long long) j * r;
            if (a == b) {
                printf("%lld\n", a);
                return 0;
            }
        }
    }
    printf("-1\n");
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (22, -1).