# Id 6965 — 10 Percent Discount

```c
#include <stdio.h>
int main(){
    double a,b;
    scanf("%lf %lf",&a,&b);
    printf("%.2f %.2f\n",a*0.9,b*0.9);
    return 0;
}
```

Verified: `88 23 → 79.20 20.70`; `192 67 → 172.80 60.30`