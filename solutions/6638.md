# Id 6638 — Vegetable Shop

```c
#include <stdio.h>

int main() {
    int a, b, c, d, x;
    scanf("%d%d%d%d%d", &a, &b, &c, &d, &x);
    int tot = a + b * 2 + c * 5 + d * 10;
    if (tot == x) printf("Paid\n");
    else if (tot > x) printf("Paid %d\n", tot - x);
    else printf("Not Paid %d\n", tot);
    return 0;
}
```

Verified: `2 5 10 5 /112 → Paid`, `5 16 3 12 /150 → Paid 22`, `10 13 15 20 /411 → Not Paid 311`
