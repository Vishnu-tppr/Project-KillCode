# Id 7156 — Diagonal Constant Matrix

```c
#include <stdio.h>

int main() {
    int r, c; scanf("%d %d", &r, &c);
    int m[50][50];
    for (int i = 0; i < r; i++)
        for (int j = 0; j < c; j++) scanf("%d", &m[i][j]);
    int flag = 1;
    for (int d = -(c - 1); d <= r - 1 && flag; d++) {
        int first = -1, have = 0;
        for (int i = 0; i < r; i++) {
            int j = i - d;
            if (j >= 0 && j < c) {
                if (!have) { first = m[i][j]; have = 1; }
                else if (m[i][j] != first) { flag = 0; break; }
            }
        }
    }
    printf(flag ? "YES\n" : "NO\n");
    return 0;
}
```

Verified: `4 3\n11 25 65\n13 11 25\n65 13 11\n25 56 13 → NO` PASS. Sample1 (`3 4\n1 2 3 4\n5 1 2 3\n9 5 1 2 → YES`) confirmed manually (recorded sample1 input contains a stray `&nbsp;` HTML entity that breaks scanf).