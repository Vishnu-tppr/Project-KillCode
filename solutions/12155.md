# Id 12155 — Washing Machine

```c
#include <stdio.h>

int main() {
    long long W;
    scanf("%lld", &W);
    if (W == 0) {
        printf("0\n");
        return 0;
    }
    if (W < 0) {
        printf("INVALID INPUT\n");
        return 0;
    }
    if (W > 7000) {
        printf("OVERLOADED\n");
        return 0;
    }
    if (W <= 2000)
        printf("25\n");
    else if (W <= 4000)
        printf("35\n");
    else
        printf("45\n");
    return 0;
}
```

Verified: samples 1/2/3 all PASS via verify.py (2000->25, 7500->OVERLOADED, -5000->INVALID INPUT).
