# Id 6857 — Remove First and Last Characters

```c
#include <stdio.h>
#include <string.h>
int main(){
    int n; int i;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        char s[101];
        scanf("%s",s);
        int len=strlen(s);
        s[len-1]='\0';
        printf("%s\n",s+1);
    }
    return 0;
}
```

Verified: `3\napple mango orange → ppl/ang/rang`; `2\nPROGRAM !@#$%^&*( → ROGRA/@#$%^&*` (recorded sample 1 also appends an editorial "Explanation:" block not in the real program output)