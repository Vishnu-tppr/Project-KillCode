# Id 2528 — String - Remove First &amp; Last Characters

```c
#include <stdio.h>
#include <string.h>
int main() {
    char s[201];
    scanf("%s", s);
    s[strlen(s) - 1] = '\0';
    printf("%s\n", s + 1);
    return 0;
}
```

2 → Lion: io, jug: u
