# Id 7045 — Except Smallest Integer

```c
#include <stdio.h>
int main(){
    int a,b,c,m;
    scanf("%d %d %d",&a,&b,&c);
    m=a; if(b<m)m=b; if(c<m)m=c;
    int vals[3]={a,b,c}, first=1;
    for(int i=0;i<3;i++){
        if(vals[i]==m) continue;
        if(!first) printf(" ");
        printf("%d",vals[i]);
        first=0;
    }
    return 0;
}
```

Verified: 43 55 23 → 43 55 ; 56 22 98 → 56 98