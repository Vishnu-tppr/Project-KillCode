# Id 6683 — getarraysum Logical Error CTS PATTERN

```c
int getArraySum(int *arr, int LENGTH) {
    int sum = 0;
    for (int i = 0; i < LENGTH; i++) {
        sum += arr[i];
    }
    return sum;
}
```

Verified: no sample I/O provided (logical-error fix); compiled with gcc -O2 and harness-tested: getArraySum({1,2,3,4,5},5)=15, getArraySum({-4,0,7},3)=3, getArraySum({},0)=0