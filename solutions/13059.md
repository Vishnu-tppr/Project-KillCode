# Id 13059 — function mergeTwoArrays

```c
#include <stdio.h>

void mergeTwoArrays(int SIZE, int arr1[], int arr2[], char *fileName) {
    FILE *fp = fopen(fileName, "w");
    int first = 1;
    for (int i = 0; i < SIZE; i++)
        for (int k = 0; k < 2; k++) {
            if (!first) fprintf(fp, " ");
            fprintf(fp, "%d", k ? arr2[i] : arr1[i]);
            first = 0;
        }
    fclose(fp);
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: arr1={10,20,30,40,50}, arr2={11,15,19,12,13} → "10 11 20 15 30 19 40 12 50 13", matching sample