# Id 6615 — Type of Processor

```c
#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);
    if (x == 25000) printf("I3\n");
    else if (x > 25000 && x < 50000) printf("I5\n");
    else printf("I7\n");
    return 0;
}
```

Verified: `25000 → I3`, `100000 → I7`
