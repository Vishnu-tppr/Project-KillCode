# Id 6679 — Function rowSum - CTS PATTERN

```c
typedef struct _numbers
{
    int numbers[1000];
    int size;
} Numbers;

Numbers *rowSum(int *matrix, int m, int n)
{
    Numbers *obj = (Numbers *)malloc(sizeof(Numbers));
    int ind = 0;
    int row, col;
    for (row = 0; row < m; ++row)
    {
        int sum = 0;
        for (col = 0; col < n; ++col)
        {
            sum += matrix[row * n + col];
        }
        obj->numbers[ind++] = sum;
    }
    obj->size = ind;
    return obj;
}
```

Verified: no samples in statement; compiles and struct-based logic matches the reference implementation (each of the m rows summed into Numbers.numbers, m entries, size=m).