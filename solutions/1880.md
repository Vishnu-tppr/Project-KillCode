# Id 1880 — Employee - Name Age Salary Asterisk

```c
#include <stdio.h>
int main() {
    char name[101];
    int age;
    double salary;
    scanf("%s %d %lf", name, &age, &salary);
    printf("%s\n**\n%d\n**\n%.2f\n", name, age, salary);
    return 0;
}
```

Verified sample I/O: Ramesh / 32 / 25010.56 → Ramesh, **, 32, **, 25010.56
