# Id 14490 — Supermarket - Last Customer Waiting Time

```c
#include <stdio.h>

int main() {
    long long N, T, X;
    scanf("%lld%lld%lld", &N, &T, &X);
    long long w = (N - 1) * (X - T);
    if (w < 0) w = 0;
    printf("%lld\n", w);
    return 0;
}
```

Verified: samples 1/2 match via verify.py.