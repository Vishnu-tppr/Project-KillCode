# Id 5412 — C - Function - Product of A and B

```c
#include <stdio.h>
void printProduct(long long A, long long B) {
    printf("%lld", A * B);
}
int main() {
    long long a, b;
    scanf("%lld%lld", &a, &b);
    printProduct(a, b);
    printf("\n");
    return 0;
}
```

Verified: `4 3` → `12`; `9 9` → `81`