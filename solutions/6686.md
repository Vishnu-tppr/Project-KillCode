# Id 6686 — removeElement from Array - CTS PATTERN

```c
int *removeElement(int *arr, int LENGTH, int index)
{
    if (index < 0 || index >= LENGTH)
        return arr;
    int *res = (int *)malloc((LENGTH - 1) * sizeof(int));
    int i, j = 0;
    for (i = 0; i < LENGTH; i++)
        if (i != index)
            res[j++] = arr[i];
    return res;
}
```

Verified: no samples in statement; compiles, removes element at given index (in-bounds) by returning a new array with it removed, and returns the input array for any out-of-bounds index.