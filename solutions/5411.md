# Id 5411 — C - Function - Sum of Two Numbers

```c
#include <stdio.h>
void printSum(int a, int b) {
    printf("%d", a + b);
}
int main() {
    int a, b;
    scanf("%d%d", &a, &b);
    printSum(a, b);
    printf("\n");
    return 0;
}
```

Verified: (no sample I/O provided; compiled with gcc -w)