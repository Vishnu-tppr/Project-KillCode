# Id 14529 — Video Game Winner

```c
#include <stdio.h>
#include <string.h>

int main() {
    char s[101];
    int n;
    scanf("%s", s);
    scanf("%d", &n);
    int a = 0, b = 0;
    int len = (int) strlen(s);
    int i = 0;
    while (i < len) {
        if (s[i] == 'a') a++;
        else b++;
        i++;
        if (a == n - 1 && b == n - 1) {
            int da = 0, db = 0, j;
            for (j = 0; j < 3 && i + j < len; j++) {
                if (s[i + j] == 'a') da++;
                else db++;
            }
            printf("Player %c\n", da > db ? 'A' : 'B');
            return 0;
        }
        if (a == n) {
            printf("Player A\n");
            return 0;
        }
        if (b == n) {
            printf("Player B\n");
            return 0;
        }
    }
    return 0;
}
```

Verified: samples 1/2/3 PASS via verify.py (Player A, Player B, Player A).