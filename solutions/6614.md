# Id 6614 — WaterTemperature

```c
#include <stdio.h>

int main() {
    char ch;
    scanf("%c", &ch);
    if (ch == 'L') printf("Hot water\n");
    else if (ch == 'R') printf("Cold water\n");
    else if (ch == 'M') printf("Moderate water\n");
    else printf("TURN ON\n");
    return 0;
}
```

Verified: `L → Hot water` (recorded sample output additionally contains the text of the next example block, which the program is not required to print)
