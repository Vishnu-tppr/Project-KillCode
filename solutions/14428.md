# Id 14428 — Clever Monkeys

```c
#include <stdio.h>

int main() {
    long long N, A, B, X, Y;
    scanf("%lld", &N);
    scanf("%lld", &A);
    scanf("%lld", &B);
    scanf("%lld", &X);
    scanf("%lld", &Y);
    long long eaten = X / A + Y / B;
    if (X % A + Y % B > 0 && eaten < N)
        eaten++;
    long long left = N - eaten;
    if (left < 0)
        left = 0;
    printf("%lld\n", left);
    return 0;
}
```

Verified: samples 1/2/3 all PASS via verify.py (eat = X/A + Y/B full meals + last monkey eats remainder).