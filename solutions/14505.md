# Id 14505 — Find Price - Costliest Jewelry Set

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    long long a[101];
    int i;
    for (i = 0; i < N; i++) scanf("%lld", &a[i]);
    long long mx = a[0], run = a[0];
    for (i = 1; i < N; i++) {
        if (a[i] > a[i - 1]) run += a[i];
        else run = a[i];
        if (run > mx) mx = run;
    }
    printf("%lld\n", mx);
    return 0;
}
```

Verified: samples 1/2 match via verify.py (360, 57).