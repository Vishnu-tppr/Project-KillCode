# Id 6967 — Print the Character

```c
#include <stdio.h>
int main(){
    char S[101], ch;
    scanf("%s", S);
    while(getchar() != '\n');
    ch = getchar();
    printf("%c", ch);
    return 0;
}
```

Verified: `hello\nh → h`