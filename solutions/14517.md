# Id 14517 — function containsKDigits

```c
void containsKDigits(long long N, int K)
{
    int digits = 0;
    long long n = N;
    while (n) { digits++; n /= 10; }
    if (digits == K)
        printf("True %d\n", digits);
    else
        printf("False %d\n", digits);
}
```

Verified: samples 1/2 all match via harness (True 5, False 3).