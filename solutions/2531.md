# Id 2531 — HCF/GCD of Two Numbers

```c
#include <stdio.h>
int main() {
    int x, y;
    scanf("%d %d", &x, &y);
    while (y) {
        int t = x % y;
        x = y;
        y = t;
    }
    printf("%d\n", x);
    return 0;
}
```

2 → 30\n40: 10, 15\n10: 5
