# Id 7018 — Previous Alphabet

```c
#include <stdio.h>
int main(){
    char ch;
    scanf("%c",&ch);
    if(ch=='\n') scanf("%c",&ch);
    printf("%c",(ch-'a'-1+26)%26+'a');
    return 0;
}
```

Verified: `a → z`; `h → g`