# Id 6588 — Simple Interest Calculation

```c
#include <stdio.h>

int main() {
    double P, R;
    int N;
    scanf("%lf%lf%d", &P, &R, &N);
    printf("%.2f\n", P * N * R / 100.0);
    return 0;
}
```

Verified: `10000 12 5 → 6000.00`, `1000 5 2 → 100.00`
