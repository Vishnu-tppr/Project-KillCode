# Id 14371 — Count Digits - Exclude D

```c
#include <stdio.h>

int main() {
    char s[12];
    int D;
    scanf("%s %d", s, &D);
    int c = 0;
    for (int i = 0; s[i]; i++)
        if (s[i] - '0' != D) c++;
    printf("%d\n", c);
    return 0;
}
```

Verified: samples 1/2 match via verify.py.