# Id 6890 — Position of Characters - X

```c
#include <stdio.h>
#include <string.h>
int main(){
    char s[101]; long long x;
    scanf("%s %lld",s,&x);
    int len=strlen(s);
    int start = (x%2==1)?1:2;
    for(int i=start;i<=len;i+=2)putchar(s[i-1]);
    printf("\n");
    return 0;
}
```

Verified: `vaticancity 67 → vtcniy`; `paris 8 → ai`