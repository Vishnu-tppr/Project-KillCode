# Id 7122 — String Modification

```c
#include <stdio.h>

int isVowel(char c) { return c=='a'||c=='e'||c=='i'||c=='o'||c=='u'; }

int main() {
    char s[101];
    scanf("%s", s);
    for (int i = 0; s[i]; i++) {
        if (!isVowel(s[i])) printf(".%c", s[i]);
    }
    printf("\n");
    return 0;
}

```

Verified: hello -> .h.l.l; skillrack -> .s.k.l.l.r.c.k
