# Id 6832 — Longest String

```c
#include <stdio.h>
#include <string.h>
int main(){
    char a[1000],b[1000];
    scanf("%s %s",a,b);
    if(strlen(a)>=strlen(b))printf("%s\n",a);
    else printf("%s\n",b);
    return 0;
}
```

Verified: `green banana → banana`; `Note Book → Note` (recorded sample 1 also appends an editorial "Explanation:" block not in the real program output)