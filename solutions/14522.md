# Id 14522 — Electricity Bill Amount

```c
#include <stdio.h>

int main() {
    long long x;
    scanf("%lld", &x);
    long long total;
    if (x <= 100) total = x * 6;
    else if (x <= 200) total = 600 + (x - 100) * 9;
    else if (x <= 300) total = 600 + 900 + (x - 200) * 15;
    else total = 600 + 900 + 1500 + (x - 300) * 20;
    printf("%lld\n", total);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (1257, 390).