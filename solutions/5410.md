# Id 5410 — C - Function - Print Twice the Value

```c
#include <stdio.h>
void doubleValue(int N) {
    printf("%d", 2 * N);
}
int main() {
    int n;
    scanf("%d", &n);
    doubleValue(n);
    printf("\n");
    return 0;
}
```

Verified: `22` → `44`; `45` → `90`