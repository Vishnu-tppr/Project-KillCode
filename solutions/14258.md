# Id 14258 — Next Letter Equal Distance

```c
char nextLetter(char ch1, char ch2)
{
    int nxt = ch2 + (ch2 - ch1);
    while (nxt > 'z') nxt -= 26;
    return (char)nxt;
}
```

Verified: samples 1/2 all match via harness (k, c).