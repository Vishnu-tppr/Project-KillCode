# Id 14164 — Hiring a Car - Travel Cost

```c
#include <stdio.h>

int main() {
    long long R1, N, R2, X;
    scanf("%lld %lld %lld %lld", &R1, &N, &R2, &X);
    long long h = (X + 59) / 60;
    long long cost;
    if (h <= N) cost = h * R1;
    else cost = N * R1 + (h - N) * R2;
    printf("%lld\n", cost);
    return 0;
}
```

Verified: samples 1/2/3 match via verify.py (120, 290, 30).