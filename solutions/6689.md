# Id 6689 — Function drawPrintpattern - CTS PATTERN

```c
void drawPrintpattern(int num) {
    for (int i = 1; i <= num; i++) {
        for (int j = 0; j < 2 * i; j++) {
            printf("1");
        }
        printf("\n");
    }
}
```

Verified: sample 1 PASS via harness (pattern for num=4)