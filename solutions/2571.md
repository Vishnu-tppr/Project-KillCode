# Id 2571 — Top Scoring Batsman Name

```c
#include <stdio.h>
#include <string.h>

int main() {
    int n;
    scanf("%d\n", &n);
    char name[101], best[101] = "";
    int runs, maxr = -1;
    for (int i = 0; i < n; i++) {
        scanf(" %[^,],%d", name, &runs);
        if (runs > maxr) { maxr = runs; strcpy(best, name); }
    }
    printf("%s\n", best);
    return 0;
}
```

Verified against sample I/O: `5 → BatsmanA,45 … BatsmanE,78` → `BatsmanE`