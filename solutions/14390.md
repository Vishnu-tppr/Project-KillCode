# Id 14390 — function findNextGreater

```c
char *findNextGreater(int L, char *numStr) {
    int i, j;
    for (i = L - 2; i >= 0; i--) {
        if (numStr[i] < numStr[i + 1]) break;
    }
    for (j = L - 1; j > i; j--) {
        if (numStr[j] > numStr[i]) break;
    }
    {
        char t = numStr[i];
        numStr[i] = numStr[j];
        numStr[j] = t;
    }
    for (i = i + 1, j = L - 1; i < j; i++, j--) {
        char t = numStr[i];
        numStr[i] = numStr[j];
        numStr[j] = t;
    }
    return numStr;
}
```

Verified: samples 1/2 all match via harness (218, 536479).