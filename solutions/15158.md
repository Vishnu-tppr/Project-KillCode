# Id 15158 — Add One Chocolate - N Boxes

```c
#include <stdio.h>

int main() {
    int N, i;
    scanf("%d", &N);
    static int a[1000005];
    for (i = 0; i < N; i++) scanf("%d", &a[i]);
    i = N - 1;
    while (i >= 0) {
        a[i]++;
        if (a[i] == 10) {
            a[i] = 0;
            i--;
        } else {
            break;
        }
    }
    if (i < 0) printf("-1\n");
    else printf("%d\n", i + 1);
    return 0;
}
```

Verified: samples 1/2/3 PASS via verify.py (3, -1, 10).