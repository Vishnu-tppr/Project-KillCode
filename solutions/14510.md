# Id 14510 — function findTotalFeet

```c
int findTotalFeet(int SIZE, int *arr) {
    int total = 0;
    int i;
    for (i = 0; i < SIZE; i++)
        total += arr[i] / 12;
    return total;
}
```

Verified: samples 1/2 match via harness (5, 16).