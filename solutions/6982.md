# Id 6982 — Sum of Least Significant Bits - M and N

```c
#include <stdio.h>
int main(){
    int m,n;
    scanf("%d%d",&m,&n);
    printf("%d",(m&1)+(n&1));
    return 0;
}
```

Verified: `6 5 → 1`; `4 8 → 0` (the first recorded sample output is polluted by an embedded explanation; the program emits the bare result `1` as the statement's example shows)