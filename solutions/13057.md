# Id 13057 — function mergeFileContents

```c
#include <stdio.h>

void mergeFileContents(char *F1, char *F2, char *F3) {
    FILE *f1 = fopen(F1, "r"), *f2 = fopen(F2, "r"), *f3 = fopen(F3, "w");
    char buf[1024];
    while (fgets(buf, sizeof(buf), f1)) fputs(buf, f3);
    while (fgets(buf, sizeof(buf), f2)) fputs(buf, f3);
    fclose(f1); fclose(f2); fclose(f3);
}
```

Verified: logic checked — merges files F1 then F2 into F3 in order (e.g. "x/y" + "p/q" → "x/y/p/q"); the recorded sample output is descriptive prose, not function output; compiled with gcc -w