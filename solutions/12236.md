# Id 12236 — Wall Painting Cost

```c
#include <stdio.h>

int main() {
    int X, Y, M, N;
    scanf("%d %d", &X, &Y);
    scanf("%d %d", &M, &N);
    double intro = 0.0, ext = 0.0;
    double a;
    for (int i = 0; i < M; i++) {
        scanf("%lf", &a);
        intro += a;
    }
    for (int i = 0; i < N; i++) {
        scanf("%lf", &a);
        ext += a;
    }
    double total = intro * X + ext * Y;
    printf("%.2f\n", total);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (1842.00, 942.00)