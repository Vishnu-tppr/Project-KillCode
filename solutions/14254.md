# Id 14254 — function multiplyBy11

```c
void multiplyBy11(char *num) {
    int len = 0;
    while (num[len] != '\0') len++;
    char res[1005];
    int pos = 0, carry = 0;
    for (int i = len - 1; i >= 0; i--) {
        int cur = (num[i] - '0') * 11 + carry;
        res[pos++] = (char)('0' + cur % 10);
        carry = cur / 10;
    }
    while (carry > 0) {
        res[pos++] = (char)('0' + carry % 10);
        carry /= 10;
    }
    while (pos > 1 && res[pos - 1] == '0') pos--;
    for (int i = pos - 1; i >= 0; i--) {
        printf("%c", res[i]);
    }
    printf("\n");
}
```

Verified: samples 1/2 all PASS via harness (275, 461505179186008629773252899748473734527588091714736); also checked large leading-digit cases manually