# Id 12221 — Water Tank & Buckets

```c
#include <stdio.h>

int main() {
    long long X, Y, v;
    scanf("%lld %lld", &X, &Y);
    long long sum = 0;
    long long b = 0;
    while (scanf("%lld", &v) == 1) {
        b++;
        sum += v;
        if (sum * 5 >= X * 4) {
            printf("%lld\n", b);
            return 0;
        }
    }
    printf("TANK NOT FULL\n");
    return 0;
}
```

Verified: samples 1/2/3 all match (5, TANK NOT FULL, 8).
