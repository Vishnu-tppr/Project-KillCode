# Id 12234 — Physical Fitness Test

```c
#include <stdio.h>

int main() {
    int val[9];
    int i;
    for (i = 0; i < 9; i++)
        scanf("%d", &val[i]);
    int sum[3];
    sum[0] = val[0] + val[3] + val[6];
    sum[1] = val[1] + val[4] + val[7];
    sum[2] = val[2] + val[5] + val[8];
    int max = sum[0];
    if (sum[1] > max) max = sum[1];
    if (sum[2] > max) max = sum[2];
    for (i = 0; i < 3; i++)
        if (sum[i] == max)
            printf("Candidate Number: %d\n", i + 1);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (ties print both candidates).