# Id 6989 — Difference Between Two Time Periods

```c
#include <stdio.h>
int main(){
    int h1,m1,h2,m2;
    scanf("%d:%d %d:%d",&h1,&m1,&h2,&m2);
    int t1=h1*60+m1, t2=h2*60+m2, d=t2-t1;
    printf("%d:%02d",d/60,d%60);
    return 0;
}
```

Verified: `9:45\n10:59 → 1:14` (recorded sample output is polluted by the merged text of Example #2; code reproduces the stated spec with `%02d` minutes padding as in `2:04`)