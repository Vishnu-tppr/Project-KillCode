# Id 7023 — Valid Character

```c
#include <stdio.h>
int main(){
    char x,y,z;
    scanf(" %c %c %c",&x,&y,&z);
    int a=x-'a'+1,b=y-'a'+1,c=z-'a'+1;
    if(c==b-a) printf("YES");
    else printf("NO");
    return 0;
}
```

Verified: e i d → YES ; g r j → NO