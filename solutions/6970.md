# Id 6970 — ASCII Value of Character

```c
#include <stdio.h>
int main(){
    char ch;
    scanf("%c",&ch);
    if(ch=='\n') scanf("%c",&ch);
    printf("%d",ch);
    return 0;
}
```

Verified: `a → 97`; `M → 77`