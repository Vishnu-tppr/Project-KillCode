# Id 6652 — Predict Rain

```c
#include <stdio.h>

int main() {
    int h, t;
    scanf("%d%d", &h, &t);
    if (h > 90 || t < 18 || (h > 70 && t < 30) || (h > 60 && t < 24))
        printf("Yes\n");
    else
        printf("No\n");
    return 0;
}
```

Verified: `75 24 → Yes`, `80 30 → No`
