# Id 14355 — function calcTotalTax

```c
long long calcTotalTax(int N, long long *amounts) {
    long long total = 0;
    for (int i = 0; i < N; i++) {
        if (amounts[i] > 1000) {
            total += (amounts[i] - 1000) / 10;
        }
    }
    return total;
}
```

Verified: samples 1/2 all PASS via harness (1000, 900011809); long long used since amounts reach 9000000000