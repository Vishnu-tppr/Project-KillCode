# Id 7149 — Column with Most Vowels

```c
#include <stdio.h>
#include <string.h>

int main() {
    int r, c; scanf("%d %d", &r, &c);
    char m[100][100]; int v[100] = {0};
    for (int i = 0; i < r; i++)
        for (int j = 0; j < c; j++) {
            scanf(" %c", &m[i][j]);
            if (strchr("aeiou", m[i][j])) v[j]++;
        }
    int best = 0;
    for (int j = 1; j < c; j++) if (v[j] > v[best]) best = j;
    for (int i = 0; i < r; i++) printf("%c\n", m[i][best]);
    return 0;
}
```

Verified: `6 5\na n e i b... → a / u / k / k / i / l` PASS (the recorded sample output additionally contains a prose Explanation; the program outputs just the column, matching); `7 3\ne q m... → q / i / k / b / e / c / o` PASS