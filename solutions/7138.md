# Id 7138 — N Format In Matrix

```c
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    char m[50][50];
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            scanf(" %c", &m[i][j]);
    for (int i = 0; i < n; i++) printf("%c", m[i][0]);
    for (int i = 1; i < n; i++) printf("%c", m[i][i]);
    for (int i = n - 2; i >= 0; i--) printf("%c", m[i][n-1]);
    printf("\n");
    return 0;
}

```

Verified: 4\nr t u o\nk l a d\nj h d g\nv b m z -> rkjvldzgdo\nExplanation:\nr t u o\nk l a d\nj h d g\nv b m z; 5\nk o E n f\nv g Y j M\nz d R t g\nn J I s x\nM l P q S -> kvznMgRsSxgMf
