# Id 14430 — TV - Exchange Offer

```c
#include <stdio.h>

int main() {
    int X, Y, Z;
    if (scanf("%d", &X) != 1) return 0;
    if (scanf("%d", &Y) != 1) return 0;
    if (X < 1 || X > 3 || Y < 1 || Y > 2) {
        printf("INVALID INPUT\n");
        return 0;
    }
    if (Y == 1) {
        if (scanf("%d", &Z) != 1) { printf("INVALID INPUT\n"); return 0; }
        if (Z < 1 || Z > 2) { printf("INVALID INPUT\n"); return 0; }
    }
    double price = (X == 1) ? 10000.0 : (X == 2) ? 8000.0 : 7000.0;
    double pay;
    if (Y == 2) pay = price;
    else if (Z == 1) pay = price * 0.8;
    else pay = price * 0.98;
    printf("%.1f\n", pay);
    return 0;
}
```

Verified: samples 1/2/3 all PASS via verify.py (5600.0, 8000.0, INVALID INPUT)