# Id 7691 — Sum of Right Side Element(s)

```c
#include <stdio.h>

int main() {
    int n; scanf("%d", &n);
    long long a[10000], s[10001];
    s[n] = 0;
    for (int i = 0; i < n; i++) scanf("%lld", &a[i]);
    for (int i = n - 1; i >= 0; i--) s[i] = s[i + 1] + a[i];
    for (int i = 0; i < n; i++) {
        if (i) printf(" ");
        printf("%lld", s[i + 1]);
    }
    printf("\n");
    return 0;
}
```

Verified: `5\n10 20 30 40 50 → 140 120 90 50 0` PASS (recorded Explanation excluded); `6\n12 8 22 48 15 25 → 118 110 88 40 25 0` PASS