# Id 14415 — Robot - Pick Steel Rods

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    long long arr[100000];
    int i;
    for (i = 0; i < N; i++)
        scanf("%lld", &arr[i]);
    long long out[100000];
    int cnt = 0;
    for (i = 0; i < N; i++) {
        if (cnt == 0 || arr[i] <= out[cnt - 1])
            out[cnt++] = arr[i];
    }
    for (i = 0; i < cnt; i++) {
        if (i > 0)
            printf(" ");
        printf("%lld", out[i]);
    }
    printf("\n");
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (survivors = elements not greater than last kept).