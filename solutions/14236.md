# Id 14236 — Placement Season - Problem Solving Capability

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    int a[1000];
    for (int i = 0; i < N; i++) scanf("%d", &a[i]);
    for (int i = 0; i < N; i++) {
        int c = 0;
        for (int j = 0; j < i; j++)
            if (a[j] > a[i]) c++;
        if (i) printf(" ");
        printf("%d", c);
    }
    printf("\n");
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (0 0 2 2 0, 0 1 1 1 1 1)