# Id 11873 — function getFactorial

```c
int getFactorial(int n) {
    int r = 1;
    for (int i = 2; i <= n; i++) r *= i;
    return r;
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: getFactorial(5)=120, getFactorial(0)=1