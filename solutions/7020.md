# Id 7020 — Divisibility of Integers

```c
#include <stdio.h>
int main(){
    long long a,b,c;
    scanf("%lld%lld%lld",&a,&b,&c);
    printf((a*b*c)%(a+b+c)==0?"Yes":"No");
    return 0;
}
```

Verified: `2 3 5 → Yes`; `1 2 4 → No`