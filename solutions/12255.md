# Id 12255 — Count & Remove Vowels

```c
#include <stdio.h>
#include <string.h>

int main() {
    char s[1001], t[1001], res[1001];
    fgets(s, sizeof(s), stdin);
    int ca = 0, ce = 0, ci = 0, co = 0, cu = 0;
    int i, k = 0;
    for (i = 0; s[i] != '\0' && s[i] != '\n'; i++) {
        char c = s[i];
        if (c == 'a' || c == 'A') ca++;
        else if (c == 'e' || c == 'E') ce++;
        else if (c == 'i' || c == 'I') ci++;
        else if (c == 'o' || c == 'O') co++;
        else if (c == 'u' || c == 'U') cu++;
        if (c != 'a' && c != 'e' && c != 'i' && c != 'o' && c != 'u'
            && c != 'A' && c != 'E' && c != 'I' && c != 'O' && c != 'U') {
            t[k++] = c;
        }
    }
    t[k] = '\0';
    /* collapse multiple spaces into single space and trim edges */
    int n = 0, j;
    i = 0;
    while (i < k) {
        while (i < k && t[i] == ' ') i++;
        if (i >= k) break;
        if (n > 0) res[n++] = ' ';
        while (i < k && t[i] != ' ') res[n++] = t[i++];
    }
    res[n] = '\0';
    printf("a: %d\n", ca);
    printf("e: %d\n", ce);
    printf("i: %d\n", ci);
    printf("o: %d\n", co);
    printf("u: %d\n", cu);
    if (n == 0) printf("-1\n");
    else printf("%s\n", res);
    return 0;
}
```

Verified: samples 1/2/3 PASS via verify.py.