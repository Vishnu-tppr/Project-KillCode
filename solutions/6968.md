# Id 6968 — String with Colon

```c
#include <stdio.h>
int main(){
    char S[101];
    fgets(S, 101, stdin);
    for(char *p=S; *p!='\n' && *p!='\0'; p++)
        printf("%c", *p);
    printf(":");
    printf("%s", S);
    return 0;
}
```

Verified: `Good Morning → Good Morning:Good Morning` (recorded sample output is polluted by the merged text of Example #2, so the top example is what is verified; code reproduces the stated spec)