# Id 14384 — Neon Number

```c
#include <stdio.h>

int main() {
    long long n;
    scanf("%lld", &n);
    long long s = 0, x = n;
    while (x > 0) {
        s += x % 10;
        x /= 10;
    }
    long long sq = s * s;
    if (sq == n)
        printf("True\n");
    else
        printf("%lld\n", sq);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (True, 36).