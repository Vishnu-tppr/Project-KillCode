# Id 6593 — Interchanged Unit Digits

```c
#include <stdio.h>

int main() {
    int x, y;
    scanf("%d%d", &x, &y);
    int nx = x / 10 * 10 + y % 10;
    int ny = y / 10 * 10 + x % 10;
    printf("%d %d\n", nx, ny);
    return 0;
}
```

Verified: `234 456 → 236 454`, `234 679 → 239 674`, `9 7 → 7 9`
