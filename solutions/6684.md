# Id 6684 — Function findMinElement - CTS PATTERN

```c
int *findMinElement(int *arr1, int M, int *arr2, int N)
{
    sortArray(arr1, M);
    sortArray(arr2, N);
    int *newArr = (int *)malloc(sizeof(int) * 2);
    newArr[0] = arr1[0];
    newArr[1] = arr2[0];
    return newArr;
}
```

Verified: harness sanity check (returns both minima via 2-element int array, sorted with provided sortArray helper); matches the reference implementation. No samples in batch.