# Id 11868 — function getIndex

```c
int getIndex(char *S, char CH) {
    for (int i = 0; S[i]; i++) if (S[i] == CH) return i;
    return -1;
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: getIndex("hello",'l')=2, getIndex("abc",'z')=-1