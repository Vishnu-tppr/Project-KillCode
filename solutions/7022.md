# Id 7022 — Unit Digit or Tenth Digit

```c
#include <stdio.h>
int main(){
    int N;
    scanf("%d",&N);
    int unit=N%10, tenth=(N/10)%10;
    if(unit>tenth) printf("%d",unit);
    else printf("%d",tenth);
    return 0;
}
```

Verified: 1234 → 4 ; 5463 → 6