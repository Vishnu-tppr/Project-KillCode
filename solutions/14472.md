# Id 14472 — Crossings Count - Vehicles

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    long long zeros = 0;
    long long crossings = 0;
    int i;
    for (i = 0; i < N; i++) {
        int v;
        scanf("%d", &v);
        if (v == 0)
            zeros++;
        else
            crossings += zeros;
    }
    printf("%lld\n", crossings);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (each south vehicle crosses all north vehicles before it).