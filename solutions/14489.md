# Id 14489 — Mixture - Find Milk Quantity

```c
#include <stdio.h>

int main() {
    double M, T;
    scanf("%lf %lf", &M, &T);
    double milk = (M - T) * (M - T) / M;
    printf("%.2f\n", milk);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (3.60, 19.36)