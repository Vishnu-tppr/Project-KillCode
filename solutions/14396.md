# Id 14396 — Numberic Code to Alphabetic Code

```c
#include <stdio.h>

int main() {
    char s[12];
    scanf("%s", s);
    for (int i = 0; s[i]; i++)
        printf("%c", 'a' + (s[i] - '0'));
    printf("\n");
    return 0;
}
```

Verified: samples 1/2 match via verify.py.