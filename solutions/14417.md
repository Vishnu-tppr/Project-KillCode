# Id 14417 — function shiftCipher

```c
char *shiftCipher(char *str) {
    for (int i = 0; str[i] != '\0'; i++) {
        str[i] = (char)('a' + (str[i] - 'a' + 3) % 26);
    }
    return str;
}
```

Verified: samples 1/2 all PASS via harness (square, skillrack)