# Id 1874 — Athlete &amp; Medals Count

```c
#include <stdio.h>
int main() {
    char n[101];
    int m;
    scanf("%s %d", n, &m);
    printf("%s won %d medals\n", n, m);
    return 0;
}
```

Verified sample I/O: Phelps / 5 → Phelps won 5 medals
