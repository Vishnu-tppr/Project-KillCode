# Id 14485 — function getRemainder

```c
int getRemainder(char *str, int K) {
    int rem = 0;
    int i;
    for (i = 0; str[i]; i++)
        rem = (rem * 10 + (str[i] - '0')) % K;
    return rem;
}
```

Verified: samples 1/2 match via harness (5089919531231 % 5 = 1, 2456758695 % 7 = 6).