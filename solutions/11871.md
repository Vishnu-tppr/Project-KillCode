# Id 11871 — function getFactorsCount

```c
int getFactorsCount(int n) {
    if (n < 0) n = -n;
    int c = 0;
    for (int i = 1; i <= n; i++) if (n % i == 0) c++;
    return c;
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: getFactorsCount(6)=4, getFactorsCount(1)=1