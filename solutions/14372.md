# Id 14372 — Total Enemies Destroyed - Sniper Game

```c
#include <stdio.h>

int main() {
    long long n, m;
    scanf("%lld", &n);
    scanf("%lld", &m);
    long long total = m * n + 3 * m * (m - 1) / 2;
    printf("%lld\n", total);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (38, 130).