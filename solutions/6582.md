# Id 6582 — Table Marked Price

```c
#include <stdio.h>
int main() {
    long long x, y;
    scanf("%lld%lld", &x, &y);
    printf("%lld\n", x * 100 / (100 - y));
    return 0;
}
```

Verified: `5225 5` → `5500`; `250 0` → `250`