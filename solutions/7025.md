# Id 7025 — Previous and Next Alphabets

```c
#include <stdio.h>
int main(){
    char ch;
    scanf(" %c",&ch);
    char prev=ch=='a'?'z':ch-1;
    char next=ch=='z'?'a':ch+1;
    printf("%c %c",prev,next);
    return 0;
}
```

Verified: a → z b ; c → b d