# Id 14194 — Cricket Tournament - Maximum Matches

```c
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    if (n < 2) {
        printf("MATCH NOT POSSIBLE\n");
        return 0;
    }
    int ans = 0;
    while (n > 1) {
        ans++;
        n = (n + 1) / 2;
    }
    printf("%d\n", ans);
    return 0;
}
```

Verified: samples 1/2/3 PASS via verify.py (3, 2, MATCH NOT POSSIBLE).