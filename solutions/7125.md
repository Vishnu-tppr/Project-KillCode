# Id 7125 — Alphabet at Index

```c
#include <stdio.h>
#include <string.h>

int main() {
    char s[101];
    scanf("%s", s);
    int len = strlen(s);
    for (int i = 0; i < len; i++) {
        int p = s[i] - 'a' + 1;
        printf("%c", s[p-1]);
    }
    printf("\n");
    return 0;
}

```

Verified: sdjfwuafvbdsjklweuficwbfyr -> ffbubcsuwdffbdsbwcuvjbduyu\nExplanation:\nFor the 1 st alphabet s , so the 19 th alphabet in the string f is printed.\nFor the 2 nd alphabet d , so the 4 th alphabet in the string f is printed.\nFor the 3 rd alphabet j , so the 10 th alphabet in the string b is printed.\nFor the 4 th alphabet f , so the 6 th alphabet in the string u is printed.\nFor the 5 th alphabet w , so the 23 rd alphabet in the string b is printed.\nSimilarly, for the remaining alphabets the corresponding alphabets are printed.\nHence the output is ffbubcsuwdffbdsbwcuvjbduyu; gcufogcecvuznmvpsyzzpjmqkievvhei -> cupgvcuoujpimnjpzkiipvnsucojjeoc
