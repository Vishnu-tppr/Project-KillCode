# Id 14411 — Jingo Lottery Game

```c
#include <stdio.h>
#include <limits.h>

int main() {
    int N;
    scanf("%d", &N);
    long long a[100000];
    for (int i = 0; i < N; i++) scanf("%lld", &a[i]);
    long long max1 = LLONG_MIN, max2 = LLONG_MIN;
    long long min1 = LLONG_MAX, min2 = LLONG_MAX;
    int imax1 = -1, imax2 = -1, imin1 = -1, imin2 = -1;
    for (int i = 0; i < N; i++) {
        if (a[i] > max1) { max2 = max1; imax2 = imax1; max1 = a[i]; imax1 = i; }
        else if (a[i] > max2) { max2 = a[i]; imax2 = i; }
        if (a[i] < min1) { min2 = min1; imin2 = imin1; min1 = a[i]; imin1 = i; }
        else if (a[i] < min2) { min2 = a[i]; imin2 = i; }
    }
    long long p1 = max1 * max2, p2 = min1 * min2;
    if (p1 >= p2) printf("%lld\n", max1 + max2);
    else printf("%lld\n", min1 + min2);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (19, -25)