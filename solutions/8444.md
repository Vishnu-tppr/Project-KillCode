# Id 8444 — Maximum Count Integer-Even or Odd

```c
#include <stdio.h>

int main() {
    int n; scanf("%d", &n);
    int a[1000], e = 0, o = 0;
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        if ((a[i] % 2 + 2) % 2 == 0) e++; else o++;
    }
    int first = 1;
    if (e > o) {
        for (int k = 0; k < 2; k++)
            for (int i = 0; i < n; i++)
                if ((a[i] % 2 + 2) % 2 == k) {
                    if (!first) printf(" ");
                    printf("%d", a[i]); first = 0;
                }
    } else {
        for (int k = 1; k >= 0; k--)
            for (int i = 0; i < n; i++)
                if ((a[i] % 2 + 2) % 2 == k) {
                    if (!first) printf(" ");
                    printf("%d", a[i]); first = 0;
                }
    }
    printf("\n");
    return 0;
}
```

Verified: `5\n41 57 65 84 23 → 41 57 65 23 84` PASS (recorded Explanation excluded); `7\n59 64 72 76 147 45 78 → 64 72 76 78 59 147 45` PASS