# Id 8442 — Even followed by Odd integers

```c
#include <stdio.h>

int main() {
    int n; scanf("%d", &n);
    int a[1000];
    for (int i = 0; i < n; i++) scanf("%d", &a[i]);
    int first = 1;
    for (int k = 0; k < 2; k++)
        for (int i = 0; i < n; i++)
            if ((a[i] % 2 + 2) % 2 == k) {
                if (!first) printf(" ");
                printf("%d", a[i]); first = 0;
            }
    printf("\n");
    return 0;
}
```

Verified: `5\n23 46 78 10 255 → 46 78 10 23 255` PASS (recorded Explanation excluded); `4\n44 78 95 62 → 44 78 62 95` PASS