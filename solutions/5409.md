# Id 5409 — C - Function - Print Square

```c
#include <stdio.h>
int getSquare(int N) {
    return N * N;
}
int main() {
    int n;
    scanf("%d", &n);
    printf("%d\n", getSquare(n));
    return 0;
}
```

Verified: (no sample I/O provided; compiled with gcc -w)