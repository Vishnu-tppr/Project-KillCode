# Id 14476 — Maximum Guests - Meeting Hall

```c
#include <stdio.h>

int main() {
    int t, i;
    scanf("%d", &t);
    int in[1000], out[1000];
    for (i = 0; i < t; i++)
        scanf("%d", &in[i]);
    for (i = 0; i < t; i++)
        scanf("%d", &out[i]);
    int cur = 0, mx = 0;
    for (i = 0; i < t; i++) {
        cur += in[i] - out[i];
        if (cur < 0) cur = 0;
        if (cur > mx) mx = cur;
    }
    printf("%d\n", mx);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (8, 3).