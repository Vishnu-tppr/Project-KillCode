# Id 7039 — Alphabet in Range

```c
#include <stdio.h>
int main(){
    char a,b,c;
    scanf(" %c %c %c",&a,&b,&c);
    char lo=a<b?a:b, hi=a>b?a:b;
    if(c>=lo&&c<=hi) printf("Yes");
    else printf("No");
    return 0;
}
```

Verified: c u p → Yes ; e a t → No