# Id 14420 — function findNthTerm - GP

```c
double findNthTerm(double X, double Y, int N)
{
    double r = Y / X;
    double res = X;
    int i;
    for (i = 1; i < N; i++)
        res *= r;
    return res;
}
```

Verified: samples 1/2 all match via harness (16.0, 86.8).