# Id 14414 — Weather Today

```c
#include <stdio.h>

int main() {
    int N;
    long long X, Y;
    scanf("%d %lld %lld", &N, &X, &Y);
    int first = 1, any = 0;
    int i;
    for (i = 0; i < N; i++) {
        long long v;
        scanf("%lld", &v);
        if (v < X || v > Y) {
            if (!first) printf(" ");
            printf("%lld", v);
            first = 0;
            any = 1;
        }
    }
    if (!any) printf("-1");
    printf("\n");
    return 0;
}
```

Verified: samples 1/2/3 match via verify.py (3 2 9 10; -6 9 -8 8 -6 -8; -1).