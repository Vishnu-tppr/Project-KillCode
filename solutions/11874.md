# Id 11874 — function compareLength

```c
int compareLength(char *a, char *b) {
    int la = 0, lb = 0;
    while (a[la]) la++;
    while (b[lb]) lb++;
    return la > lb ? 1 : (la == lb ? 0 : -1);
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: ("abc","ab")=1, ("ab","ab")=0, ("a","ab")=-1