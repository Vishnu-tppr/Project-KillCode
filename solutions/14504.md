# Id 14504 — Gift Hamper

```c
#include <stdio.h>

int main() {
    long long n, i, v;
    scanf("%lld", &n);
    long long total = 0;
    for (i = 0; i < n; i++) {
        scanf("%lld", &v);
        total += v;
    }
    long long c = 1;
    while (c * c * c < total) c++;
    if (c * c * c == total)
        printf("YES\n");
    else
        printf("%lld\n", c * c * c - total);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (YES, 30).