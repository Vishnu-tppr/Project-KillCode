# Id 15157 — function getCommonDigit

```c
int getCommonDigit(int X, int Y, int Z)
{
    int a[10] = {0}, b[10] = {0}, c[10] = {0};
    while (X > 0) {
        a[X % 10] = 1;
        X /= 10;
    }
    while (Y > 0) {
        b[Y % 10] = 1;
        Y /= 10;
    }
    while (Z > 0) {
        c[Z % 10] = 1;
        Z /= 10;
    }
    int d;
    for (d = 0; d < 10; d++)
        if (a[d] && b[d] && c[d])
            return d;
    return -1;
}
```

Verified: samples 1/2/3 all match via harness (637 271 277 -> 7; 731 153 375 -> 3; 963 123 456 -> -1).