# Id 14173 — Sports Car Racing

```c
#include <stdio.h>

int main() {
    long long N1, N2, X;
    scanf("%lld%lld%lld", &N1, &N2, &X);
    if (N2 <= N1) {
        printf("-1\n");
        return 0;
    }
    long long diff = N2 - N1;
    long long t = X / diff + 1;
    printf("%lld\n", t);
    return 0;
}
```

Verified: samples 1/2/3 match via verify.py.