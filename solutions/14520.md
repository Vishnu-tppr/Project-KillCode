# Id 14520 — Pollution Control Rule - Total Fine

```c
#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    int reg[100];
    for (int i = 0; i < N; i++) scanf("%d", &reg[i]);
    int D, X;
    scanf("%d", &D);
    scanf("%d", &X);
    int dp = D % 2;
    long long fine = 0;
    for (int i = 0; i < N; i++) {
        if (reg[i] % 2 != dp) fine += X;
    }
    printf("%lld\n", fine);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (450, 2500)