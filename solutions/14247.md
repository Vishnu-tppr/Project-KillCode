# Id 14247 — function remainderMod11

```c
int remainderMod11(char *input1) {
    int rem = 0;
    int i;
    for (i = 0; input1[i]; i++)
        rem = (rem * 10 + (input1[i] - '0')) % 11;
    return rem;
}
```

Verified: samples 1/2/3/4 match via harness (0, 5, 9, 4).