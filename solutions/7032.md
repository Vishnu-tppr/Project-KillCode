# Id 7032 — Divisible or Not

```c
#include <stdio.h>
int main(){
    int N;
    scanf("%d",&N);
    if(N%6==0 && (N%4==0||N%8==0)) printf("Yes");
    else printf("No");
    return 0;
}
```

Verified: 60 → Yes