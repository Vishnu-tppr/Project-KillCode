# Id 2527 — Second Largest Value among N integers

```c
#include <stdio.h>
int main() {
    int n, max1 = -1000000, max2 = -1000000;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        int v;
        scanf("%d", &v);
        if (v > max1) {
            max2 = max1;
            max1 = v;
        } else if (v > max2 && v != max1) {
            max2 = v;
        }
    }
    printf("%d\n", max2);
    return 0;
}
```

Verified sample I/O: 3 (100 2200 345) → 345; 6 (-23 -256 -87 -90 -11019 -2) → -23
