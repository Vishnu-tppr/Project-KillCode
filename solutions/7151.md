# Id 7151 — Diagonally Dominant or Not

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n; scanf("%d", &n);
    int m[50][50];
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++) scanf("%d", &m[i][j]);
    int flag = 1;
    for (int i = 0; i < n && flag; i++) {
        int s = 0;
        for (int j = 0; j < n; j++) if (j != i) s += abs(m[i][j]);
        if (abs(m[i][i]) < s) flag = 0;
    }
    if (!flag) printf("No\n");
    else
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (j) printf(" ");
                printf("%d", m[i][j]);
            }
            printf("\n");
        }
    return 0;
}
```

Verified: `3\n3 -2 1... → 3 -2 1 / 1 -3 -1 / 2 3 7` matches (recorded output has trailing Explanation prose excluded); `4\n4 -1 -1 -1\n2 -6 2 3\n1 2 4 1\n2 3 1 -5 → No` PASS