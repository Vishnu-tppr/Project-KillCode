# Id 12513 — Chemical Mixture - Explosion Rate

```c
#include <stdio.h>

int main() {
    int N, i;
    scanf("%d", &N);
    long long a[1000], b[1000];
    for (i = 0; i < N; i++) scanf("%lld", &a[i]);
    for (i = 0; i < N; i++) scanf("%lld", &b[i]);
    long long sum = 0;
    for (i = 0; i < N; i++) {
        long long p = a[i] * b[N - 1 - i];
        if (p > 0) sum += p;
    }
    printf("%lld\n", sum);
    return 0;
}
```

Verified: samples 1/2 PASS via verify.py (58, 185).