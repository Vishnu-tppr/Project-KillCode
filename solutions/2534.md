# Id 2534 — Sum of Tenth and Unit Digits

```c
#include <stdio.h>
int main() {
    long long n;
    scanf("%lld", &n);
    printf("%lld\n", (n / 10) % 10 + n % 10);
    return 0;
}
```

3 → 231: 4, 100: 0, 192: 11
