# Id 6869 — Replace Spaces in S

```c
#include <stdio.h>
int main(){
    int c;
    while((c=getchar())!=EOF){
        if(c==' ')printf("#@*");
        else putchar(c);
    }
    return 0;
}
```

Verified: `The sun rises in the east → The#@*sun#@*rises#@*in#@*the#@*east`; `hello → hello`