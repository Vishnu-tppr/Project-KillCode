# Id 14400 — function createNum

```c
int createNum(int a, int b, int c)
{
    int t;
    if (a < b) { t = a; a = b; b = t; }
    if (b < c) { t = b; b = c; c = t; }
    if (a < b) { t = a; a = b; b = t; }
    if (a < c) { t = a; a = c; c = t; }
    if (b < c) { t = b; b = c; c = t; }
    return (100 * a) + (10 * b) + c;
}
```

Verified: samples 1/2 all match via harness (851, 0).