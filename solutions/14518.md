# Id 14518 — Maximum Flower Plants - Garden

```c
#include <stdio.h>

int main() {
    long long R, C;
    scanf("%lld %lld", &R, &C);
    printf("%lld\n", (R * C + 1) / 2);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (checkerboard placement -> ceil(R*C/2)).