# Id 7019 — Swap the Digits

```c
#include <stdio.h>
int main(){
    int m,n;
    scanf("%d%d",&m,&n);
    if(m>n){
        m=(m%10)*10+m/10;
    }else{
        n=(n%10)*10+n/10;
    }
    printf("%d",m+n);
    return 0;
}
```

Verified: `19 28 → 101`; `45 12 → 66`