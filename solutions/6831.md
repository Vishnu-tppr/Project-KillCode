# Id 6831 — Sort Two String Values

```c
#include <stdio.h>
#include <string.h>
int main(){
    char a[1000],b[1000];
    scanf("%s %s",a,b);
    if(strcmp(a,b)<=0)printf("%s %s\n",a,b);
    else printf("%s %s\n",b,a);
    return 0;
}
```

Verified: `yacht amoeba → amoeba yacht`; `track trek → track trek` (recorded sample 2 also appends an editorial "Explanation:" block not part of the real program output)