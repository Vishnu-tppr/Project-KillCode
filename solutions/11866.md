# Id 11866 — function getVowelsCount

```c
int getVowelsCount(char *s) {
    int c = 0;
    for (; *s; s++) {
        char x = *s;
        if (x == 'a' || x == 'e' || x == 'i' || x == 'o' || x == 'u' ||
            x == 'A' || x == 'E' || x == 'I' || x == 'O' || x == 'U') c++;
    }
    return c;
}
```

Verified: (no sample I/O provided; follows stated spec) compiled with gcc -w; logic checked: "hello"=2, "AEIOUbc"=5