# Id 1879 — Space Separated String Input

```c
#include <stdio.h>
int main() {
    char s[101];
    fgets(s, sizeof(s), stdin);
    printf("%s", s);
    return 0;
}
```

1 → Good Morning. Please have a cup of coffee.: Good Morning. Please have a cup of coffee.
