# Id 12366 — Customer & Queries

```c
#include <stdio.h>

int main() {
    int C, Q;
    scanf("%d %d", &C, &Q);
    long long items[51] = {0};
    int first = 1;
    int q;
    for (q = 0; q < Q; q++) {
        int type, a, b;
        scanf("%d %d %d", &type, &a, &b);
        if (type == 1) {
            items[a] += b;
        } else {
            long long sum = 0;
            int i;
            for (i = a; i <= b; i++)
                sum += items[i];
            if (!first)
                printf(" ");
            printf("%lld", sum);
            first = 0;
        }
    }
    printf("\n");
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (type-2 results printed space-separated on one line).