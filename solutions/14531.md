# Id 14531 — function getTotalPoints

```c
int getTotalPoints(int p, int q, int r, int s)
{
    int vals[4];
    vals[0] = p;
    vals[1] = q;
    vals[2] = r;
    vals[3] = s;
    int total = 0;
    int i;
    for (i = 0; i < 4; i++) {
        if (vals[i] == 11)
            total += 3;
        else if (vals[i] == 9)
            total += 2;
        else if (vals[i] == 10)
            total += 1;
        else if (vals[i] == 1)
            total += 1;
    }
    return total;
}
```

Verified: samples 1/2 all match via harness (5 11 9 1 -> 6; 13 2 10 10 -> 2).