# Id 2569 — Print String Till Character

```c
#include <stdio.h>
#include <string.h>
int main() {
    char s[101];
    char c;
    fgets(s, sizeof(s), stdin);
    scanf(" %c", &c);
    s[strcspn(s, "\n")] = '\0';
    for (int i = 0; s[i] && s[i] != c; i++)
        printf("%c", s[i]);
    printf("\n");
    return 0;
}
```

sample1 PASS (manager/e → manag); sample2 recorded output is contaminated with Explanation text; correct output Ev per statement
