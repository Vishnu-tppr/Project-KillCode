# Id 6859 — Longest Word

```c
#include <stdio.h>
#include <string.h>
int main(){
    char s[1002];
    fgets(s,1002,stdin);
    s[strcspn(s,"\n")]=0;
    char best[1002]=""; int blen=0;
    char cur[1002]=""; int clen=0;
    int len=strlen(s);
    for(int i=0;i<=len;i++){
        if(s[i]==' '||s[i]=='\0'){
            if(clen>blen){strcpy(best,cur);blen=clen;}
            cur[0]='\0';clen=0;
        }else{
            cur[clen++]=s[i];cur[clen]='\0';
        }
    }
    printf("%s\n",best);
    return 0;
}
```

Verified: `Good Morning → Morning`; `good time → good` (recorded sample 1 also appends an editorial "Explanation:" block not in the real program output)