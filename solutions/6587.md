# Id 6587 — Circumference of the Circle

```c
#include <stdio.h>
int main() {
    int d;
    scanf("%d", &d);
    printf("%.3f\n", 22.0 / 7.0 * d);
    return 0;
}
```

Verified: `20` → `62.857`; `18` → `56.571`