# Id 14426 — Hotel - Order Food Online

```c
#include <stdio.h>

int main() {
    const double price[] = {0, 80.0, 130.0, 95.0, 80.0, 90.0,
                            110.0, 120.0, 140.0, 70.0, 80.0};
    double total = 0;
    int item, qty;
    char ch;
    while (scanf("%d", &item) == 1) {
        scanf("%d", &qty);
        scanf(" %c", &ch);
        if (item < 1 || item > 10 || qty < 1 || qty > 10) {
            printf("INVALID INPUT\n");
            return 0;
        }
        total += price[item] * qty;
        if (ch == 'N' || ch == 'n')
            break;
    }
    printf("%.1f\n", total);
    return 0;
}
```

Verified: samples 1/2/3 PASS via verify.py (680.0, 1190.0, INVALID INPUT).