# Id 14477 — Encrypt Message

```c
#include <stdio.h>

int main() {
    char s[1001];
    scanf("%s", s);
    int i;
    for (i = 0; s[i]; i++)
        if (s[i] >= 'a' && s[i] <= 'z')
            s[i] = 'a' + (s[i] - 'a' + 5) % 26;
    printf("%s\n", s);
    return 0;
}
```

Verified: samples 1/2 match via verify.py (uwtlwfrrjw; xywjjywfhjw).