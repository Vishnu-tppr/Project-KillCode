# Id 12511 — Nuclear Reaction - Total Energy

```c
#include <stdio.h>

int main() {
    long long E, R, T;
    scanf("%lld %lld %lld", &E, &R, &T);
    long long total = T * E + R * (T - 1) * T / 2;
    printf("%lld\n", total);
    return 0;
}
```

Verified: samples 1/2 match via verify.py (48, 40).