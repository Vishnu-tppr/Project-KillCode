# Id 6589 — Precision upto 3 decimal places

```c
#include <stdio.h>

int main() {
    int x, y;
    scanf("%d%d", &x, &y);
    printf("%.3f\n", (double)x / y);
    return 0;
}
```

Verified: `5 3 → 1.667`, `10 8 → 1.250`
