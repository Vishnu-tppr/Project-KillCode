# Id 7117 — Alphabets Digits and Symbols

```c
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main() {
    char s[1001];
    fgets(s, sizeof(s), stdin);
    s[strcspn(s, "\n")] = 0;
    char a[1001], d[1001], y[1001];
    int ia = 0, id = 0, iy = 0;
    for (int i = 0; s[i]; i++) {
        if (isalpha(s[i])) a[ia++] = s[i];
        else if (isdigit(s[i])) d[id++] = s[i];
        else y[iy++] = s[i];
    }
    a[ia] = d[id] = y[iy] = 0;
    printf("%s%s%s\n", a, d, y);
    return 0;
}

```

Verified: 12Man?Won -> ManWon12?\nExplanation:\nThe alphabets in &quot;12Man?Won&quot; are ManWon\nThe digits in &quot;12Man?Won&quot; are 12\nThe symbols in &quot;12Man?Won&quot; is ?\nHence the output is ManWon12? is printed.; ?,goal22fight?lost -> goalfightlost22?,?
