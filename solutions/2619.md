# Id 2619 — Sub Palindromes

```c
#include <stdio.h>
#include <string.h>
int main() {
    char s[201];
    scanf("%s", s);
    int len = (int)strlen(s), count = 0;
    for (int i = 0; i < len; i++)
        for (int j = i + 1; j < len; j++) {
            int ok = 1;
            for (int a = i, b = j; a < b; a++, b--)
                if (s[a] != s[b]) { ok = 0; break; }
            if (ok) count++;
        }
    printf("%d\n", count);
    return 0;
}
```

Verified: `everest` → 2; `abccbaab` → 5 (recorded sample output appends explanation/second-example text; program matches the stated output)