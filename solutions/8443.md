# Id 8443 — Largest Unit Digits Integers

```c
#include <stdio.h>

int main() {
    int n; scanf("%d", &n);
    int a[1000], md = -1;
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        int u = a[i] % 10; if (u < 0) u = -u;
        if (u > md) md = u;
    }
    int first = 1;
    for (int i = 0; i < n; i++) {
        int u = a[i] % 10; if (u < 0) u = -u;
        if (u == md) {
            if (!first) printf(" ");
            printf("%d", a[i]); first = 0;
        }
    }
    printf("\n");
    return 0;
}
```

Verified: `5\n41 57 65 84 27 → 57 27` PASS (recorded Explanation excluded); `6\n57 78 148 9685 321 458 → 78 148 458` PASS