# Id 12340 — Barcode Number - Old ID

```c
#include <stdio.h>

int main() {
    long long n;
    scanf("%lld", &n);
    int mn = 9;
    if (n == 0) mn = 0;
    while (n > 0) {
        int d = n % 10;
        if (d < mn) mn = d;
        n /= 10;
    }
    printf("%d\n", mn);
    return 0;
}
```

Verified: samples 1/2 all PASS via verify.py (2, 5)