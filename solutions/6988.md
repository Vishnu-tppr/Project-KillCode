# Id 6988 — Time Taken to Cover Distance

```c
#include <stdio.h>
int main(){
    double sp,dist;
    scanf("%lf%lf",&sp,&dist);
    printf("%.2f",dist/sp);
    return 0;
}
```

Verified: `60 180 → 3.00`; `25 135 → 5.40`